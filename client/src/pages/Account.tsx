import React, { FormEvent, useState } from "react";
import { Link } from "wouter";
import { KeyRound, LogIn, LogOut, ShieldCheck, ShoppingBag, UserRound } from "lucide-react";
import { startLogin } from "@/const";
import { CartDrawer, StoreFooter, StoreHeader } from "@/components/storefront";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";

export default function Account() {
  const { user, loading, isAuthenticated, logout } = useAuth();
  const utils = trpc.useUtils();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [currentPassword, setCurrentPassword] = useState("");
  const [newPassword, setNewPassword] = useState("");
  const signIn = trpc.localAuth.signIn.useMutation({ onSuccess: () => utils.auth.me.invalidate() });
  const changePassword = trpc.localAuth.changePassword.useMutation({ onSuccess: () => { setCurrentPassword(""); setNewPassword(""); utils.auth.me.invalidate(); } });

  const submitSignIn = async (event: FormEvent) => {
    event.preventDefault();
    await signIn.mutateAsync({ email, password });
  };
  const submitPasswordChange = async (event: FormEvent) => {
    event.preventDefault();
    await changePassword.mutateAsync({ currentPassword, newPassword });
  };

  return <div className="min-h-screen bg-[#fcfbf8]"><StoreHeader /><main className="container py-16 sm:py-24"><div className="mx-auto max-w-2xl rounded-[2rem] border border-stone-200 bg-white p-7 shadow-sm sm:p-10"><p className="text-[10px] font-black uppercase tracking-[0.18em] text-[#a76c45]">Your account</p>{loading ? <div className="mt-5 space-y-3"><div className="h-8 w-2/3 animate-pulse rounded bg-stone-100" /><div className="h-4 w-full animate-pulse rounded bg-stone-100" /></div> : !isAuthenticated ? <><h1 className="mt-3 font-serif text-4xl font-black">Welcome back</h1><p className="mt-4 max-w-lg text-sm leading-7 text-stone-600">Sign in with your store email and password to access your account. Administrator permissions are verified on the server.</p><form onSubmit={submitSignIn} className="mt-7 grid gap-4"><label className="grid gap-2 text-sm font-bold text-stone-700">Email<input value={email} onChange={event => setEmail(event.target.value)} type="email" autoComplete="email" required className="rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-[#a76c45]" /></label><label className="grid gap-2 text-sm font-bold text-stone-700">Password<input value={password} onChange={event => setPassword(event.target.value)} type="password" autoComplete="current-password" required className="rounded-xl border border-stone-200 px-4 py-3 font-normal outline-none focus:border-[#a76c45]" /></label>{signIn.error && <p role="alert" className="text-sm text-rose-700">{signIn.error.message}</p>}<button disabled={signIn.isPending} className="inline-flex items-center justify-center gap-2 rounded-full bg-stone-900 px-5 py-3 text-xs font-black uppercase tracking-[0.12em] text-white hover:bg-[#a76c45] disabled:opacity-50"><LogIn size={15} /> {signIn.isPending ? "Signing in" : "Sign in"}</button></form><div className="mt-6 border-t border-stone-100 pt-5"><button onClick={startLogin} className="text-xs font-bold text-stone-500 hover:text-[#a76c45]">Or continue with your Manus account</button></div></> : <><div className="mt-6 flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-full bg-[#f2eee7] text-[#a76c45]"><UserRound size={25} /></div><div><h1 className="font-serif text-3xl font-black">{user?.name || "Your profile"}</h1><p className="text-sm text-stone-500">{user?.email || "Signed in customer"}</p></div></div>{user?.requiresPasswordChange && <section className="mt-8 rounded-2xl border border-amber-200 bg-amber-50 p-5"><div className="flex gap-3"><KeyRound className="shrink-0 text-amber-700" size={19} /><div><h2 className="font-serif text-xl font-black">Change your temporary password</h2><p className="mt-1 text-sm leading-6 text-stone-600">Create a password with at least 10 characters, including letters and numbers, before continuing with administrator work.</p></div></div><form onSubmit={submitPasswordChange} className="mt-5 grid gap-3"><input value={currentPassword} onChange={event => setCurrentPassword(event.target.value)} type="password" autoComplete="current-password" placeholder="Current password" required className="rounded-xl border border-stone-200 bg-white px-4 py-3 text-sm outline-none focus:border-[#a76c45]" /><input value={newPassword} onChange={event => setNewPassword(event.target.value)} type="password" autoComplete="new-password" placeholder="New password" required className="rounded-xl border border-stone-200 bg-white px-4 py-3 text-sm outline-none focus:border-[#a76c45]" />{changePassword.error && <p role="alert" className="text-sm text-rose-700">{changePassword.error.message}</p>}<button disabled={changePassword.isPending} className="justify-self-start rounded-full bg-stone-900 px-5 py-3 text-xs font-bold text-white disabled:opacity-50">{changePassword.isPending ? "Updating" : "Update password"}</button></form></section>}<div className="mt-8 grid gap-3 sm:grid-cols-2"><Link href="/shop" className="rounded-2xl border border-stone-200 p-5 hover:border-[#a76c45]"><ShoppingBag className="text-[#a76c45]" size={20} /><p className="mt-4 font-serif text-xl font-black">Continue shopping</p><p className="mt-1 text-xs leading-5 text-stone-500">Browse current product collections.</p></Link>{user?.role === "admin" && <Link href="/admin" className="rounded-2xl border border-stone-200 p-5 hover:border-[#a76c45]"><ShieldCheck className="text-[#a76c45]" size={20} /><p className="mt-4 font-serif text-xl font-black">Administration</p><p className="mt-1 text-xs leading-5 text-stone-500">Open protected operational controls.</p></Link>}</div><button onClick={() => logout()} className="mt-8 inline-flex items-center gap-2 text-xs font-black uppercase tracking-[0.12em] text-stone-500 hover:text-rose-600"><LogOut size={14} /> Sign out</button></>}</div></main><StoreFooter /><CartDrawer /></div>;
}
