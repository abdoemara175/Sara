import React, { FormEvent, useState } from "react";
import { ShieldCheck, Trash2, UserPlus, UserRoundCog } from "lucide-react";
import { trpc } from "@/lib/trpc";

export function UserManagement({ currentUserId }: { currentUserId: number }) {
  const utils = trpc.useUtils();
  const users = trpc.userAdmin.list.useQuery();
  const [email, setEmail] = useState("");
  const [name, setName] = useState("");
  const [password, setPassword] = useState("");
  const [role, setNewUserRole] = useState<"admin" | "user">("user");
  const refresh = () => utils.userAdmin.list.invalidate();
  const create = trpc.userAdmin.create.useMutation({ onSuccess: () => { setEmail(""); setName(""); setPassword(""); refresh(); } });
  const setMemberRole = trpc.userAdmin.setRole.useMutation({ onSuccess: refresh });
  const setActive = trpc.userAdmin.setActive.useMutation({ onSuccess: refresh });
  const remove = trpc.userAdmin.delete.useMutation({ onSuccess: refresh });
  const submit = async (event: FormEvent) => { event.preventDefault(); await create.mutateAsync({ email, name: name || undefined, password, role }); };

  return <section className="mt-8 rounded-3xl border border-stone-200 bg-white p-6"><div className="flex items-start gap-3"><UserRoundCog className="mt-1 text-[#a76c45]" size={20} /><div><p className="text-[10px] font-black uppercase tracking-[0.16em] text-[#a76c45]">Access control</p><h2 className="mt-1 font-serif text-2xl font-black">Users & roles</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-stone-600">Create local customer or administrator accounts. Role and status changes are enforced by server procedures; the last active administrator cannot be removed, disabled, or demoted.</p></div></div><form onSubmit={submit} className="mt-6 grid gap-3 rounded-2xl bg-[#fcfbf8] p-4 md:grid-cols-2"><input value={name} onChange={event => setName(event.target.value)} placeholder="Name (optional)" className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-sm outline-none focus:border-[#a76c45]" /><input value={email} onChange={event => setEmail(event.target.value)} type="email" placeholder="Email" required className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-sm outline-none focus:border-[#a76c45]" /><input value={password} onChange={event => setPassword(event.target.value)} type="password" placeholder="Temporary password" required className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-sm outline-none focus:border-[#a76c45]" /><select value={role} onChange={event => setNewUserRole(event.target.value as "admin" | "user")} className="rounded-xl border border-stone-200 bg-white px-3 py-2 text-sm outline-none focus:border-[#a76c45]"><option value="user">Customer</option><option value="admin">Administrator</option></select>{create.error && <p role="alert" className="text-sm text-rose-700 md:col-span-2">{create.error.message}</p>}<button disabled={create.isPending} className="inline-flex items-center justify-center gap-2 rounded-xl bg-stone-900 px-4 py-3 text-xs font-bold text-white hover:bg-[#a76c45] disabled:opacity-50 md:col-span-2"><UserPlus size={15} /> Create user</button></form>{users.isLoading ? <div className="mt-5 h-24 animate-pulse rounded-2xl bg-stone-100" /> : users.error ? <p role="alert" className="mt-5 text-sm text-rose-700">{users.error.message}</p> : <div className="mt-6 divide-y divide-stone-100">{users.data?.map(member => <article key={member.id} className="flex flex-col gap-4 py-4 lg:flex-row lg:items-center"><div className="min-w-0 flex-1"><p className="truncate font-bold text-stone-800">{member.name || "Unnamed user"} {member.id === currentUserId && <span className="ml-1 text-xs font-normal text-stone-400">(you)</span>}</p><p className="truncate text-sm text-stone-500">{member.email || "No email"}</p><p className="mt-1 text-[10px] font-bold uppercase tracking-[0.12em] text-stone-400">{member.loginMethod || "unknown"} · {member.mustChangePassword ? "password update required" : "password current"}</p></div><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-3 py-1 text-[10px] font-black uppercase tracking-[0.1em] ${member.isActive ? "bg-emerald-50 text-emerald-700" : "bg-stone-100 text-stone-500"}`}>{member.isActive ? "Active" : "Disabled"}</span><select aria-label={`Role for ${member.email || member.id}`} value={member.role} onChange={event => setMemberRole.mutate({ userId: member.id, role: event.target.value as "admin" | "user" })} disabled={setMemberRole.isPending} className="rounded-full border border-stone-200 bg-white px-3 py-1.5 text-xs font-bold"><option value="user">Customer</option><option value="admin">Admin</option></select><button onClick={() => setActive.mutate({ userId: member.id, isActive: !member.isActive })} disabled={setActive.isPending} className="rounded-full border border-stone-200 px-3 py-1.5 text-xs font-bold hover:border-[#a76c45]">{member.isActive ? "Disable" : "Enable"}</button><button onClick={() => remove.mutate({ userId: member.id })} disabled={member.id === currentUserId || remove.isPending} aria-label={`Delete ${member.email || member.id}`} className="grid h-8 w-8 place-items-center rounded-full text-stone-400 hover:bg-rose-50 hover:text-rose-600 disabled:opacity-30"><Trash2 size={15} /></button></div></article>)}</div>}</section>;
}
